export default function FlowEditorLoading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--background)] px-8 text-[var(--foreground)]">
      <p className="text-sm text-[var(--muted)]">Loading flow...</p>
    </main>
  );
}
